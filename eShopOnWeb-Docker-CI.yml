# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

pool:
  vmImage: 'ubuntu-16.04'

resources:
  repositories:
  - repository: eShopWeb
    type: git
    name: eShopWeb
  
steps:
- task: CmdLine@2
  inputs:
    script: |
      docker login -u $(registryName) -p $(registryPassword) $(registryUrl)
      docker build . -t $(registryUrl)/ase_test2:latest
      docker push $(registryUrl)/ase_test2:latest
